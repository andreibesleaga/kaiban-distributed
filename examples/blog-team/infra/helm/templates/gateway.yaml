apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kaiban-blog-team.fullname" . }}-gateway
  labels:
    {{- include "kaiban-blog-team.labels" . | nindent 4 }}
    app: {{ include "kaiban-blog-team.fullname" . }}-gateway
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      {{- include "kaiban-blog-team.selectorLabels" . | nindent 6 }}
      app: {{ include "kaiban-blog-team.fullname" . }}-gateway
  template:
    metadata:
      labels:
        {{- include "kaiban-blog-team.selectorLabels" . | nindent 8 }}
        app: {{ include "kaiban-blog-team.fullname" . }}-gateway
    spec:
      containers:
      - name: gateway
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.gateway.port }}
        envFrom:
        - configMapRef:
            name: {{ include "kaiban-blog-team.fullname" . }}-config
        env:
        - name: AGENT_IDS
          value: "gateway"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.gateway.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.gateway.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaiban-blog-team.fullname" . }}-gateway
  labels:
    {{- include "kaiban-blog-team.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  ports:
  - port: {{ .Values.gateway.port }}
    targetPort: {{ .Values.gateway.port }}
    {{- if eq .Values.gateway.service.type "NodePort" }}
    nodePort: {{ .Values.gateway.service.nodePort }}
    {{- end }}
  selector:
    {{- include "kaiban-blog-team.selectorLabels" . | nindent 4 }}
    app: {{ include "kaiban-blog-team.fullname" . }}-gateway
