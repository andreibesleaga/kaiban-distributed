# Blog Team Agent Deployments
# Apply: kubectl apply -f agents.yaml
# All three agents share the same container image; the command distinguishes their role.

# ── Researcher ──────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-researcher
  labels:
    app: kaiban-researcher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaiban-researcher
  template:
    metadata:
      labels:
        app: kaiban-researcher
    spec:
      containers:
      - name: researcher
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/blog-team/researcher-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-blog-team-config
        - secretRef:
            name: kaiban-blog-team-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # NOTE: worker nodes consume from the queue and expose NO HTTP server, so
        # HTTP /health probes would fail and CrashLoopBackOff the pod. The
        # container's main process IS the worker — Kubernetes restarts the pod if
        # that process exits, which is the correct liveness signal for a worker.
---
# ── Writer ───────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-writer
  labels:
    app: kaiban-writer
spec:
  replicas: 1  # Scale up for parallel draft generation
  selector:
    matchLabels:
      app: kaiban-writer
  template:
    metadata:
      labels:
        app: kaiban-writer
    spec:
      containers:
      - name: writer
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/blog-team/writer-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-blog-team-config
        - secretRef:
            name: kaiban-blog-team-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # NOTE: worker nodes consume from the queue and expose NO HTTP server, so
        # HTTP /health probes would fail and CrashLoopBackOff the pod. The
        # container's main process IS the worker — Kubernetes restarts the pod if
        # that process exits, which is the correct liveness signal for a worker.
---
# ── Editor ───────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-editor
  labels:
    app: kaiban-editor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaiban-editor
  template:
    metadata:
      labels:
        app: kaiban-editor
    spec:
      containers:
      - name: editor
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/blog-team/editor-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-blog-team-config
        - secretRef:
            name: kaiban-blog-team-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # NOTE: worker nodes consume from the queue and expose NO HTTP server, so
        # HTTP /health probes would fail and CrashLoopBackOff the pod. The
        # container's main process IS the worker — Kubernetes restarts the pod if
        # that process exits, which is the correct liveness signal for a worker.
