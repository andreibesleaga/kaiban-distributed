# Searcher Agent — Fan-Out node (competing consumers)
# Scale up to handle more parallel search tasks
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-research-searcher
  labels:
    app: kaiban-research-searcher
spec:
  replicas: 4  # Default: 4 parallel searchers for fan-out
  selector:
    matchLabels:
      app: kaiban-research-searcher
  template:
    metadata:
      labels:
        app: kaiban-research-searcher
    spec:
      containers:
      - name: searcher
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/global-research/searcher-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-research-config
        - secretRef:
            name: kaiban-research-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
---
# HPA: auto-scale searcher fleet based on CPU load
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kaiban-research-searcher-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kaiban-research-searcher
  minReplicas: 2
  maxReplicas: 8
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
# Writer Agent — Fan-In aggregation node
# Synthesises all search results into a cohesive research report
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-research-writer
  labels:
    app: kaiban-research-writer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaiban-research-writer
  template:
    metadata:
      labels:
        app: kaiban-research-writer
    spec:
      containers:
      - name: writer
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/global-research/writer-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-research-config
        - secretRef:
            name: kaiban-research-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
---
# Reviewer Agent — AI Ethics & Compliance Officer
# Performs governance review against IEEE AI 7000, EU AI Act, GDPR, OWASP, NIST AI RMF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-research-reviewer
  labels:
    app: kaiban-research-reviewer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaiban-research-reviewer
  template:
    metadata:
      labels:
        app: kaiban-research-reviewer
    spec:
      containers:
      - name: reviewer
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/global-research/reviewer-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-research-config
        - secretRef:
            name: kaiban-research-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
---
# Editor Agent — Chief Research Editor (HITL gate)
# Final editorial review; triggers human-on-the-loop decision
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaiban-research-editor
  labels:
    app: kaiban-research-editor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaiban-research-editor
  template:
    metadata:
      labels:
        app: kaiban-research-editor
    spec:
      containers:
      - name: editor
        image: kaiban-distributed:latest
        imagePullPolicy: IfNotPresent
        command: ["node", "dist/examples/global-research/editor-node.js"]
        envFrom:
        - configMapRef:
            name: kaiban-research-config
        - secretRef:
            name: kaiban-research-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
